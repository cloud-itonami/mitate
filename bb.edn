{:paths ["src" "test"]
 :tasks
 {test {:doc "Run mitate cljc actor boundary tests"
        :task (do
                (require 'clojure.test
                         'mitate.murakumo-test)
                (let [run-tests (requiring-resolve 'clojure.test/run-tests)
                      r (run-tests 'mitate.murakumo-test)]
                  (System/exit (if (zero? (+ (:fail r) (:error r))) 0 1))))}}}
